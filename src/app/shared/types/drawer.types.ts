export enum EDrawerPosition {
  LEFT = 'left',
  RIGHT = 'right',
  TOP = 'top',
  BOTTOM = 'bottom',
}

export enum EDrawerDetailType {
  DATE = 'date',
  TIME = 'time',
  DURATION = 'duration',
  STATUS = 'status',
  NOTES = 'notes',
}
